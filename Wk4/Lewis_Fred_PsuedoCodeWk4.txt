// Create self-executing function// Declare Variables// Create loop // Initialize for // Generate a number up to “names.length”   // Declare variable  // Set-up person object// personIndex// Populate object into HTML element x 2 (for each column)// Push person to “people” array// Remove ”.splice” name from list to prevent repeating// “clearInterval(interval);”// Set the interval to keep the loop going until its done// Pass into the field// Run once for each element in the array// “.update”